---
phase: 04-advanced-features
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - gosysml2/sysml/model.go
  - gosysml2/sysml/parse.go
  - gosysml2/sysml/visitor.go
  - gosysml2/sysml/port_test.go
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "ConjugatedPortDefinition elements parse correctly"
    - "Port conjugation (~) syntax resolves to conjugated port"
    - "ConjugatedPort has unit tests"
  artifacts:
    - path: "gosysml2/sysml/model.go"
      provides: "ConjugatedPort struct type"
      contains: "type ConjugatedPort struct"
    - path: "gosysml2/sysml/parse.go"
      provides: "Port conjugation parser handler"
      contains: "EnterConjugatedPortDefinition|conjugated port"
    - path: "gosysml2/sysml/visitor.go"
      provides: "VisitConjugatedPort visitor method"
      contains: "VisitConjugatedPort"
    - path: "gosysml2/sysml/port_test.go"
      provides: "ConjugatedPort unit tests"
      contains: "TestConjugatedPort"
  key_links:
    - from: "parse.go port conjugation"
      to: "model.go ConjugatedPort"
      via: "NewConjugatedPort constructor"
      pattern: "NewConjugatedPort"
    - from: "model.go ConjugatedPort"
      to: "model.go Port"
      via: "reference resolution"
      pattern: "resolveConjugatedPortRefs"
---

<objective>
Close gap: ConjugatedPort implementation. KindConjugatedPort constant exists but no ConjugatedPort struct type, parser handler, or visitor support.

Purpose: Enable parsing of port conjugation syntax (~PortName) which represents an inverted/conjugated port interface.

Output: Complete ConjugatedPort type with parser handler, visitor support, reference resolution, and unit tests.
</objective>

<execution_context>
@/home/daniel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/daniel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-advanced-features/04-01-SUMMARY.md
@.planning/phases/04-advanced-features/04-advanced-features-VERIFICATION.md

# Existing code patterns
@gosysml2/sysml/model.go (KindConjugatedPort at line 42, Port struct pattern, lines 200-250)
@gosysml2/sysml/visitor.go (VisitPort pattern)
@gosysml2/sysml/parse.go (EnterPortDefinition handler)
@gosysml2/sysml/case_test.go (test patterns)

# Grammar reference
@docs/bnf/SysML-textual-bnf.kebnf (lines 639-658 for PortConjugation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ConjugatedPort type to model.go</name>
  <files>gosysml2/sysml/model.go</files>
  <action>
    Add complete ConjugatedPort type following Port pattern:
    1. Add "conjugated port" case to ElementKind.String() switch (after KindConjugatedPort)
    2. Create ConjugatedPort struct with:
       - baseElement (embedded)
       - OriginalPort Ref[*Port]
       - unresolvedOriginalPort string
       - EffectiveName string (computed as "~" + original port name)
    3. Implement isDefinition() method (returns true - ConjugatedPortDefinition is a definition)
    4. Create NewConjugatedPort(name string, loc Location) *ConjugatedPort constructor
    5. Add SetUnresolvedOriginalPort(ref string) method
    6. Add GetOriginalPort() method returning *Port
    
    Note: Per BNF, a PortDefinition always contains a nested ConjugatedPortDefinition.
    The effective name is "~" + original port name.
    
    Reference Port struct pattern in model.go.
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>ConjugatedPort type defined with all required fields and methods</done>
</task>

<task type="auto">
  <name>Task 2: Add ConjugatedPort visitor support</name>
  <files>gosysml2/sysml/visitor.go</files>
  <action>
    Add visitor pattern support for ConjugatedPort following Port pattern:
    1. Add VisitConjugatedPort(conj *ConjugatedPort) bool to Visitor interface
    2. Add VisitConjugatedPort to BaseVisitor (returns true)
    3. Add VisitConjugatedPort to Counter (increments KindConjugatedPort count)
    4. Add case *ConjugatedPort to visitElement switch statement
    5. Add FindConjugatedPorts function following FindPorts pattern
    
    Reference VisitPort implementation for pattern.
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>Visitor interface and implementations updated for ConjugatedPort</done>
</task>

<task type="auto">
  <name>Task 3: Implement port conjugation parser handler</name>
  <files>gosysml2/sysml/parse.go</files>
  <action>
    Implement port conjugation handling:
    1. Per BNF note (lines 648-651): PortDefinition always contains ConjugatedPortDefinition
    2. Modify EnterPortDefinition to also create ConjugatedPort as child:
       - After creating Port, create ConjugatedPort with name "~" + port.Name
       - Set OriginalPort reference to the main port
       - Add ConjugatedPort as child of Port
    3. Alternative: Implement EnterConjugatedPortDefinition if separate grammar rule exists
    4. Handle ~PortName syntax when parsing port references
    
    Key insight: The conjugated port is automatically created when parsing a PortDefinition.
    When parsing "port def MyPort", both MyPort and ~MyPort should exist.
    
    Reference EnterPortDefinition and EnterCaseDefinition patterns.
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>Port conjugation handler creates ConjugatedPort elements</done>
</task>

<task type="auto">
  <name>Task 4: Add ConjugatedPort reference resolution</name>
  <files>gosysml2/sysml/model.go</files>
  <action>
    Add reference resolution for ConjugatedPort:
    1. Add ResolveReferences method to ConjugatedPort:
       - Resolve OriginalPort from unresolvedOriginalPort
       - If resolved, set OriginalPort and EffectiveName fields
       - EffectiveName = "~" + OriginalPort.Name
    2. Add case *ConjugatedPort to Model.ResolveReferences switch statement
    3. Create resolveConjugatedPortRefs helper function
    
    The ConjugatedPort references the original Port it's conjugating.
    EffectiveName is computed, not stored as unresolved.
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>ConjugatedPort references resolve correctly to Port elements</done>
</task>

<task type="auto">
  <name>Task 5: Create unit tests for ConjugatedPort</name>
  <files>gosysml2/sysml/port_test.go</files>
  <action>
    Create comprehensive unit tests following case_test.go patterns:
    1. TestNewConjugatedPort - verify constructor creates element
    2. TestConjugatedPortExists - verify conjugated port auto-created with PortDefinition
    3. TestConjugatedPortResolution - verify ~Port syntax resolves
    4. TestConjugatedPortName - verify effective name is ~prefix
    5. TestConjugatedPortOriginalRef - verify OriginalPort reference works
    6. TestConjugatedPortVisitor - verify visitor pattern works
    
    Test with SysML like:
    ```
    port def MyPort;
    // ~MyPort should be auto-created as conjugated port
    ```
    
    Also test explicit usage:
    ```
    part def MyPart {
      port p : ~MyPort  // conjugated port usage
    }
    ```
    
    Follow patterns from case_test.go.
  </action>
  <verify>go test ./gosysml2/sysml/... -run TestConjugatedPort -v passes</verify>
  <done>All ConjugatedPort tests pass with comprehensive coverage</done>
</task>

</tasks>

<verification>
- [ ] ConjugatedPort struct type exists in model.go
- [ ] KindConjugatedPort has String() case returning "conjugated port"
- [ ] VisitConjugatedPort exists in Visitor interface
- [ ] BaseVisitor has VisitConjugatedPort implementation
- [ ] Counter has VisitConjugatedPort implementation
- [ ] FindConjugatedPorts function exists
- [ ] Port conjugation handler creates ConjugatedPort elements
- [ ] resolveConjugatedPortRefs resolves OriginalPort references
- [ ] Model.ResolveReferences handles *ConjugatedPort case
- [ ] port_test.go exists with passing tests
- [ ] Grammar coverage increases (1 more element handled)
</verification>

<success_criteria>
- ConjugatedPortDefinition parses without discarding elements
- Port conjugation (~Port) syntax resolves correctly
- ConjugatedPort automatically created for each PortDefinition
- All unit tests pass
- Gap closed: "ConjugatedPortDefinition elements parse correctly" truth verified
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-features/04-05-SUMMARY.md`
</output>

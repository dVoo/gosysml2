---
phase: 04-advanced-features
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - gosysml2/sysml/model.go
  - gosysml2/sysml/parse.go
  - gosysml2/sysml/visitor.go
  - gosysml2/sysml/usecase_test.go
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "IncludeUseCaseUsage elements parse and appear in model"
    - "Included use case references resolve to actual UseCase elements"
    - "IncludeUseCaseUsage has unit tests"
  artifacts:
    - path: "gosysml2/sysml/model.go"
      provides: "resolveIncludeUseCaseRefs function"
      contains: "resolveIncludeUseCaseRefs"
    - path: "gosysml2/sysml/parse.go"
      provides: "IncludeUseCaseUsage parser handler"
      contains: "EnterIncludeUseCaseUsage"
    - path: "gosysml2/sysml/visitor.go"
      provides: "VisitIncludeUseCase visitor method"
      contains: "VisitIncludeUseCase"
    - path: "gosysml2/sysml/usecase_test.go"
      provides: "IncludeUseCase unit tests"
      contains: "TestIncludeUseCase"
  key_links:
    - from: "parse.go EnterIncludeUseCaseUsage"
      to: "model.go IncludeUseCase"
      via: "NewIncludeUseCase constructor"
      pattern: "NewIncludeUseCase"
    - from: "model.go IncludeUseCase"
      to: "model.go UseCase"
      via: "reference resolution"
      pattern: "resolveIncludeUseCaseRefs"
---

<objective>
Close gap: IncludeUseCaseUsage parsing. The IncludeUseCase type exists but has no parser handler, visitor support, or reference resolution.

Purpose: Enable parsing of use case inclusion relationships ("include OtherUseCase" syntax) from SysML source.

Output: Working EnterIncludeUseCaseUsage handler, VisitIncludeUseCase visitor method, reference resolution, and comprehensive unit tests.
</objective>

<execution_context>
@/home/daniel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/daniel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-advanced-features/04-01-SUMMARY.md
@.planning/phases/04-advanced-features/04-advanced-features-VERIFICATION.md

# Existing code patterns
@gosysml2/sysml/model.go (IncludeUseCase struct at lines 907-938, UseCase pattern)
@gosysml2/sysml/visitor.go (VisitCase pattern at lines 31, 114, 164, 617-620)
@gosysml2/sysml/parse.go (EnterCaseDefinition/Usage handlers at lines 903-961)
@gosysml2/sysml/case_test.go (test patterns for reference)

# Grammar reference
@docs/bnf/SysML-textual-bnf.kebnf (lines 1568-1574 for IncludeUseCaseUsage)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IncludeUseCase visitor support</name>
  <files>gosysml2/sysml/visitor.go</files>
  <action>
    Add visitor pattern support for IncludeUseCase following the Case pattern:
    1. Add VisitIncludeUseCase(include *IncludeUseCase) bool to Visitor interface (after VisitCase)
    2. Add VisitIncludeUseCase to BaseVisitor (returns true)
    3. Add VisitIncludeUseCase to Counter (increments KindIncludeUseCase count)
    4. Add case *IncludeUseCase to visitElement switch statement
    5. Add FindIncludeUseCases function following FindCases pattern
    
    Reference existing VisitCase implementation at lines 31, 114, 164, 617-620.
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>Visitor interface and implementations updated for IncludeUseCase</done>
</task>

<task type="auto">
  <name>Task 2: Implement EnterIncludeUseCaseUsage handler</name>
  <files>gosysml2/sysml/parse.go</files>
  <action>
    Implement EnterIncludeUseCaseUsage handler following EnterCaseUsage pattern:
    1. Extract name from Identification if present
    2. Create Location from context
    3. Create IncludeUseCase using NewIncludeUseCase(name, loc)
    4. Set unresolvedIncludedUseCase from the referenced use case name
    5. Add to current UseCase's IncludedUseCases slice or push to elementStack
    6. Implement ExitIncludeUseCaseUsage to pop from elementStack
    
    The IncludeUseCaseUsage grammar rule represents "include OtherUseCase" syntax
    within a use case definition. The referenced use case name should be stored
    in unresolvedIncludedUseCase for later resolution.
    
    Pattern: Follow EnterCaseUsage at lines 933-961.
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>EnterIncludeUseCaseUsage handler creates IncludeUseCase elements</done>
</task>

<task type="auto">
  <name>Task 3: Add IncludeUseCase reference resolution</name>
  <files>gosysml2/sysml/model.go</files>
  <action>
    Add reference resolution for IncludeUseCase following resolveCaseRefs pattern:
    1. Add ResolveReferences method to IncludeUseCase:
       - Resolve IncludedUseCase from unresolvedIncludedUseCase
       - If resolved, set IncludedUseCase field
    2. Add case *IncludeUseCase to Model.ResolveReferences switch statement
    3. Create resolveIncludeUseCaseRefs helper function following resolveCaseRefs pattern
    
    The IncludeUseCase struct already has:
    - IncludedUseCase Ref[*UseCase]
    - unresolvedIncludedUseCase string
    - SetUnresolvedIncludedUseCase method
    
    Reference resolveCaseRefs at lines 1869-1901 for pattern.
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>IncludeUseCase references resolve correctly to UseCase elements</done>
</task>

<task type="auto">
  <name>Task 4: Create unit tests for IncludeUseCase</name>
  <files>gosysml2/sysml/usecase_test.go</files>
  <action>
    Create comprehensive unit tests following case_test.go patterns:
    1. TestNewIncludeUseCase - verify constructor creates element
    2. TestIncludeUseCaseUsage - parse "include OtherUseCase" syntax
    3. TestIncludeUseCaseWithReference - parse include with qualified name
    4. TestIncludeUseCaseResolution - verify reference resolves to UseCase
    5. TestUseCaseWithIncludes - parse use case with multiple includes
    6. TestIncludeUseCaseVisitor - verify visitor pattern works
    
    Test with SysML like:
    ```
    use case def BaseUseCase;
    use case def MyUseCase {
      include BaseUseCase
    }
    ```
    
    Follow patterns from case_test.go (lines 1-458).
  </action>
  <verify>go test ./gosysml2/sysml/... -run TestIncludeUseCase -v passes</verify>
  <done>All IncludeUseCase tests pass with comprehensive coverage</done>
</task>

</tasks>

<verification>
- [ ] VisitIncludeUseCase exists in Visitor interface
- [ ] BaseVisitor has VisitIncludeUseCase implementation
- [ ] Counter has VisitIncludeUseCase implementation
- [ ] FindIncludeUseCases function exists
- [ ] EnterIncludeUseCaseUsage handler creates IncludeUseCase elements
- [ ] ExitIncludeUseCaseUsage pops from elementStack
- [ ] resolveIncludeUseCaseRefs resolves IncludedUseCase references
- [ ] Model.ResolveReferences handles *IncludeUseCase case
- [ ] usecase_test.go exists with passing tests
- [ ] Grammar coverage increases (1 more element handled)
</verification>

<success_criteria>
- IncludeUseCaseUsage parses without discarding elements
- IncludeUseCase elements appear in model with correct references
- Included use case references resolve to actual UseCase elements
- All unit tests pass
- Gap closed: "IncludeUseCaseUsage elements parse and appear in model" truth verified
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-features/04-04-SUMMARY.md`
</output>

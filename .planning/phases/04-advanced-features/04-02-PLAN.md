---
phase: 04-advanced-features
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - gosysml2/sysml/model.go
  - gosysml2/sysml/parse.go
  - gosysml2/sysml/visitor.go
  - gosysml2/sysml/usecase_test.go
  - gosysml2/sysml/port_test.go
autonomous: true
must_haves:
  truths:
    - "IncludeUseCaseUsage elements parse and appear in model"
    - "UseCase includes reference resolved to actual UseCase elements"
    - "ConjugatedPortDefinition elements parse correctly"
    - "Port conjugation (~) syntax resolves to conjugated port"
    - "All new handlers have unit tests"
  artifacts:
    - path: "gosysml2/sysml/model.go"
      provides: "IncludeUseCase and ConjugatedPort types"
      contains: "type IncludeUseCase|type ConjugatedPort"
    - path: "gosysml2/sysml/parse.go"
      provides: "IncludeUseCaseUsage and PortConjugation handlers"
      contains: "EnterIncludeUseCaseUsage|EnterPortConjugation"
    - path: "gosysml2/sysml/visitor.go"
      provides: "VisitIncludeUseCase and VisitConjugatedPort methods"
      contains: "VisitIncludeUseCase|VisitConjugatedPort"
    - path: "gosysml2/sysml/usecase_test.go"
      provides: "Unit tests for IncludeUseCase"
      contains: "TestIncludeUseCase"
    - path: "gosysml2/sysml/port_test.go"
      provides: "Unit tests for Port conjugation"
      contains: "TestConjugatedPort"
  key_links:
    - from: "parse.go EnterIncludeUseCaseUsage"
      to: "model.go UseCase.IncludedUseCases"
      via: "append to slice"
      pattern: "IncludedUseCases = append"
    - from: "parse.go PortConjugation"
      to: "model.go ConjugatedPort"
      via: "conjugated port creation"
      pattern: "NewConjugatedPort"
    - from: "model.go UseCase"
      to: "model.go IncludeUseCase"
      via: "reference resolution"
      pattern: "resolveIncludedUseCases"
---

<objective>
Implement Use case relationships (IncludeUseCaseUsage) and Port conjugation (ConjugatedPortDefinition, PortConjugation) to complete P2 medium priority grammar elements.

Purpose: IncludeUseCaseUsage enables use case inclusion relationships ("include" keyword). Port conjugation enables the ~Port syntax for inverted port interfaces. These are essential for enterprise SysML models.

Output: Working IncludeUseCase and ConjugatedPort types with parser handlers, following established patterns.
</objective>

<execution_context>
@/home/daniel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/daniel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase summary for patterns
@.planning/phases/04-advanced-features/04-01-SUMMARY.md
@.planning/phases/03-grammar-completion/03-02-SUMMARY.md

# Grammar reference
@docs/bnf/SysML-textual-bnf.kebnf (lines 1568-1574 for IncludeUseCaseUsage, lines 639-658 for PortConjugation)

# Existing implementations for reference
@gosysml2/sysml/model.go (UseCase with IncludedUseCases, Port pattern)
@gosysml2/sysml/parse.go (EnterUseCaseDefinition/Usage handlers)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IncludeUseCase type to model.go</name>
  <files>gosysml2/sysml/model.go</files>
  <action>
    Add IncludeUseCase struct following the pattern of other usage types:
    1. Add KindIncludeUseCase constant to ElementKind enum
    2. Add "include use case" to String() switch
    3. Create IncludeUseCase struct with:
       - baseElement
       - IncludedUseCase Ref[*UseCase]
       - unresolvedIncludedUseCase string
       - Owner Ref[*UseCase] (the use case that includes this)
    4. Implement isUsage() method (no isDefinition - always a usage)
    5. Create NewIncludeUseCase(name, loc) constructor
    
    Note: IncludeUseCaseUsage is a special usage that represents
    the inclusion relationship, not the included use case itself.
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>IncludeUseCase type defined with required fields</done>
</task>

<task type="auto">
  <name>Task 2: Add ConjugatedPort type to model.go</name>
  <files>gosysml2/sysml/model.go</files>
  <action>
    Add ConjugatedPort struct following Port pattern:
    1. Add KindConjugatedPort constant to ElementKind enum
    2. Add "conjugated port" to String() switch
    3. Create ConjugatedPort struct with:
       - baseElement
       - OriginalPort Ref[*Port]
       - unresolvedOriginalPort string
       - EffectiveName string (computed as "~" + original port name)
    4. Implement isDefinition() and isUsage() methods
    5. Create NewConjugatedPort(name, loc) constructor
    
    Note: ConjugatedPortDefinition is always created as a child
    of a PortDefinition (per BNF note on line 648-651).
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>ConjugatedPort type defined with required fields</done>
</task>

<task type="auto">
  <name>Task 3: Add IncludeUseCase and ConjugatedPort to visitor</name>
  <files>gosysml2/sysml/visitor.go</files>
  <action>
    1. Add VisitIncludeUseCase(include *IncludeUseCase) bool to Visitor interface
    2. Add VisitConjugatedPort(conj *ConjugatedPort) bool to Visitor interface
    3. Add implementations to BaseVisitor
    4. Add implementations to Counter
    5. Add to VisitElement switch statement
    6. Add FindIncludeUseCases and FindConjugatedPorts functions
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>Visitor pattern supports new types</done>
</task>

<task type="auto">
  <name>Task 4: Implement IncludeUseCaseUsage handler</name>
  <files>gosysml2/sysml/parse.go</files>
  <action>
    Implement EnterIncludeUseCaseUsage:
    1. Parse 'include' keyword context
    2. Handle two forms per BNF:
       - Reference subsetting form: include [qualified name]
       - Declaration form: include use case [name]
    3. Create IncludeUseCase element
    4. Set unresolvedIncludedUseCase from reference
    5. Add to current UseCase's children (or separate tracking)
    
    Also implement ExitIncludeUseCaseUsage for resolution.
    
    Note: The BNF shows IncludeUseCaseUsage can have a CaseBody,
    so it may contain subject/actor/objective members.
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>IncludeUseCaseUsage handler creates elements</done>
</task>

<task type="auto">
  <name>Task 5: Implement PortConjugation handler</name>
  <files>gosysml2/sysml/parse.go</files>
  <action>
    Implement PortConjugation handling:
    1. PortConjugation is parsed as part of ConjugatedPortDefinition
    2. When parsing ~PortName syntax, create ConjugatedPort
    3. Set unresolvedOriginalPort to the referenced port name
    4. The ConjugatedPort is implicitly created when a PortDefinition is parsed
       (per BNF note: "a PortDefinition is always parsed as containing a nested ConjugatedPortDefinition")
    
    Key insight from BNF lines 648-651: The conjugated port is automatically
    created when parsing a PortDefinition. The effective name is "~" + original name.
    
    Implementation approach:
    - Modify EnterPortDefinition to also create ConjugatedPort as child
    - Set OriginalPort reference to the main port
    - Compute EffectiveName with "~" prefix
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>PortConjugation handler creates conjugated ports</done>
</task>

<task type="auto">
  <name>Task 6: Add reference resolution</name>
  <files>gosysml2/sysml/model.go</files>
  <action>
    1. Add ResolveReferences for IncludeUseCase:
       - Resolve IncludedUseCase reference
       - If resolved, append to owner UseCase's IncludedUseCases slice
    2. Add ResolveReferences for ConjugatedPort:
       - Resolve OriginalPort reference
       - Set EffectiveName to "~" + OriginalPort.Name
    3. Add to Model.ResolveReferences switch statement
    4. Add resolveIncludeUseCaseRefs and resolveConjugatedPortRefs helpers
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>References resolve correctly for new types</done>
</task>

<task type="auto">
  <name>Task 7: Create unit tests for IncludeUseCase</name>
  <files>gosysml2/sysml/usecase_test.go</files>
  <action>
    Create tests following existing patterns:
    1. TestIncludeUseCaseUsage - parse include statement
    2. TestIncludeUseCaseWithReference - parse include with qualified name
    3. TestIncludeUseCaseResolution - verify reference resolves
    4. TestUseCaseWithIncludes - parse use case with multiple includes
    
    Test with SysML like:
    ```
    use case def MyUseCase {
      include OtherUseCase
    }
    ```
  </action>
  <verify>go test ./gosysml2/sysml/... -run TestIncludeUseCase -v passes</verify>
  <done>IncludeUseCase tests pass with 100% coverage</done>
</task>

<task type="auto">
  <name>Task 8: Create unit tests for PortConjugation</name>
  <files>gosysml2/sysml/port_test.go</files>
  <action>
    Create tests following existing patterns:
    1. TestConjugatedPortExists - verify conjugated port auto-created
    2. TestConjugatedPortResolution - verify ~Port syntax resolves
    3. TestConjugatedPortName - verify effective name is ~prefix
    4. TestPortConjugationTyping - verify ~Port can be used as type
    
    Test with SysML like:
    ```
    port def MyPort;
    // ~MyPort should resolve to conjugated port
    ```
  </action>
  <verify>go test ./gosysml2/sysml/... -run TestConjugatedPort -v passes</verify>
  <done>PortConjugation tests pass with 100% coverage</done>
</task>

</tasks>

<verification>
- [ ] IncludeUseCase type exists in model.go
- [ ] ConjugatedPort type exists in model.go
- [ ] KindIncludeUseCase and KindConjugatedPort constants defined
- [ ] Visitor interface has VisitIncludeUseCase and VisitConjugatedPort
- [ ] EnterIncludeUseCaseUsage handler creates elements
- [ ] PortDefinition auto-creates ConjugatedPort child
- [ ] IncludeUseCase references resolve to UseCase elements
- [ ] ConjugatedPort references resolve and set EffectiveName
- [ ] Unit tests pass for both features
- [ ] Grammar coverage increases from ~70% to ~73% (2 more elements)
</verification>

<success_criteria>
- IncludeUseCaseUsage parses without discarding elements
- Use case inclusion relationships resolve correctly
- Port conjugation (~Port) parses and resolves
- ConjugatedPort automatically created for each PortDefinition
- All unit tests pass
- Grammar coverage reaches 73%+ (6 elements total in Phase 4)
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-features/04-02-SUMMARY.md`
</output>

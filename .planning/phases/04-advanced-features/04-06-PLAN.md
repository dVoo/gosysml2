---
phase: 04-advanced-features
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - gosysml2/sysml/model.go
  - gosysml2/sysml/parse.go
  - gosysml2/sysml/flow_test.go
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "SuccessionFlowUsage elements parse and appear in model"
    - "SuccessionFlow connects source and target elements"
    - "SuccessionFlow has unit tests"
  artifacts:
    - path: "gosysml2/sysml/model.go"
      provides: "resolveSuccessionFlowRefs function"
      contains: "resolveSuccessionFlowRefs"
    - path: "gosysml2/sysml/parse.go"
      provides: "SuccessionFlowUsage parser handler"
      contains: "EnterSuccessionFlowUsage"
    - path: "gosysml2/sysml/flow_test.go"
      provides: "SuccessionFlow unit tests"
      contains: "TestSuccessionFlow"
  key_links:
    - from: "parse.go EnterSuccessionFlowUsage"
      to: "flow.go SuccessionFlow"
      via: "NewSuccessionFlow constructor"
      pattern: "NewSuccessionFlow"
    - from: "flow.go SuccessionFlow"
      to: "Source/Target elements"
      via: "reference resolution"
      pattern: "resolveSuccessionFlowRefs"
---

<objective>
Close gap: SuccessionFlowUsage parsing. The SuccessionFlow type exists with visitor support but has no parser handler or reference resolution.

Purpose: Enable parsing of succession flow usage in action bodies for control flow modeling (distinct from already-implemented SuccessionAsUsage).

Output: Working EnterSuccessionFlowUsage handler, reference resolution for Source/Target, and comprehensive unit tests.
</objective>

<execution_context>
@/home/daniel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/daniel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-advanced-features/04-01-SUMMARY.md
@.planning/phases/04-advanced-features/04-advanced-features-VERIFICATION.md

# Existing code patterns
@gosysml2/sysml/flow.go (SuccessionFlow struct at lines 129-199, already exists)
@gosysml2/sysml/visitor.go (VisitSuccessionFlow already exists at lines 96-97, 136, 208-209, 642-645)
@gosysml2/sysml/parse.go (EnterFlowUsage pattern, EnterSuccessionAsUsage at lines 2210-2235)
@gosysml2/sysml/flow_test.go (existing Flow tests for reference)

# Grammar reference
@docs/bnf/SysML-textual-bnf.kebnf (line 829 for SuccessionFlowUsage)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement EnterSuccessionFlowUsage handler</name>
  <files>gosysml2/sysml/parse.go</files>
  <action>
    Implement EnterSuccessionFlowUsage handler following EnterFlowUsage pattern:
    1. Parse the succession flow usage context from grammar rule
    2. Extract source and target element references from the succession relationship
    3. Create SuccessionFlow element using NewSuccessionFlow(name, loc)
    4. Set unresolvedSource and unresolvedTarget from parsed references
    5. Add to current action or flow context
    6. Implement ExitSuccessionFlowUsage to pop from elementStack
    
    Note: SuccessionFlowUsage is distinct from SuccessionAsUsage (already implemented).
    SuccessionFlowUsage is used in action bodies for control flow succession,
    while SuccessionAsUsage is for general succession relationships.
    
    The BNF shows SuccessionFlowUsage at line 829:
    SuccessionFlowUsage = FlowUsagePrefix 'succession' ...
    
    Reference EnterSuccessionAsUsage at lines 2210-2235 (but create
    SuccessionFlow, not Transition).
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>EnterSuccessionFlowUsage handler creates SuccessionFlow elements</done>
</task>

<task type="auto">
  <name>Task 2: Add SuccessionFlow reference resolution</name>
  <files>gosysml2/sysml/model.go</files>
  <action>
    Add reference resolution for SuccessionFlow:
    1. Add ResolveReferences method to SuccessionFlow (in flow.go or model.go):
       - Resolve Source from unresolvedSource
       - Resolve Target from unresolvedTarget
    2. Add case *SuccessionFlow to Model.ResolveReferences switch statement
    3. Create resolveSuccessionFlowRefs helper function following resolveFlowRefs pattern
    
    The SuccessionFlow struct already has:
    - Source Ref[Element]
    - Target Ref[Element]
    - unresolvedSource string
    - unresolvedTarget string
    - SetUnresolvedSource and SetUnresolvedTarget methods
    
    Reference resolveFlowRefs in model.go for pattern.
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>SuccessionFlow Source and Target references resolve correctly</done>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for SuccessionFlow</name>
  <files>gosysml2/sysml/flow_test.go</files>
  <action>
    Add SuccessionFlow tests to existing flow_test.go:
    1. TestSuccessionFlowUsage - parse basic succession flow
    2. TestSuccessionFlowWithSourceTarget - parse with explicit source/target
    3. TestSuccessionFlowResolution - verify Source and Target references resolve
    4. TestSuccessionFlowInAction - parse succession within action body
    5. TestSuccessionFlowWithGuard - parse with optional guard condition
    
    Test with SysML like:
    ```
    action def MyAction {
      action a1;
      action a2;
      succession s from a1 to a2;
    }
    ```
    
    Or inline form:
    ```
    action def MyAction {
      action a1;
      succession from a1;
      action a2;
    }
    ```
    
    Follow patterns from existing flow_test.go tests.
  </action>
  <verify>go test ./gosysml2/sysml/... -run TestSuccessionFlow -v passes</verify>
  <done>All SuccessionFlow tests pass with comprehensive coverage</done>
</task>

<task type="auto">
  <name>Task 4: Run validation suite to verify coverage</name>
  <files>gosysml2/sysml/</files>
  <action>
    1. Run the full test suite to verify no regressions
    2. Verify grammar coverage increased (target: 73%+)
    3. Check validation success rate maintained (98%+)
    4. Document final coverage percentage
    
    Commands:
    - go test ./gosysml2/sysml/... -v
    - go test ./... (full test suite)
    
    This gap closure should bring Phase 4 to completion with all
    P2 medium priority elements implemented.
  </action>
  <verify>All tests pass, coverage at 73%+, validation success rate 98%+</verify>
  <done>Phase 4 complete with target coverage achieved</done>
</task>

</tasks>

<verification>
- [ ] EnterSuccessionFlowUsage handler creates SuccessionFlow elements
- [ ] ExitSuccessionFlowUsage pops from elementStack
- [ ] resolveSuccessionFlowRefs resolves Source and Target references
- [ ] Model.ResolveReferences handles *SuccessionFlow case
- [ ] flow_test.go has TestSuccessionFlow* tests
- [ ] All SuccessionFlow tests pass
- [ ] No regressions in existing tests
- [ ] Grammar coverage reaches 73%+ (7 elements in Phase 4)
- [ ] Validation success rate maintained at 98%+
</verification>

<success_criteria>
- SuccessionFlowUsage parses without discarding elements
- SuccessionFlow connects source and target elements correctly
- All new and existing tests pass
- Grammar coverage reaches 73%+ (up from 68%)
- Validation success rate maintained at 98%+
- Gap closed: "SuccessionFlowUsage elements parse and appear in model" truth verified
- Phase 4 complete: All P2 medium priority elements implemented
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-features/04-06-SUMMARY.md`
</output>

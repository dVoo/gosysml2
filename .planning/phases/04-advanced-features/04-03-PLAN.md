---
phase: 04-advanced-features
plan: 03
type: execute
wave: 1
depends_on: [04-01, 04-02]
files_modified:
  - gosysml2/sysml/model.go
  - gosysml2/sysml/parse.go
  - gosysml2/sysml/visitor.go
  - gosysml2/sysml/flow_test.go
autonomous: true
must_haves:
  truths:
    - "SuccessionFlowUsage elements parse and appear in model"
    - "SuccessionFlow connects source and target elements"
    - "SuccessionFlowUsage can be used in action bodies"
    - "All new handlers have unit tests"
  artifacts:
    - path: "gosysml2/sysml/model.go"
      provides: "SuccessionFlow type definition"
      contains: "type SuccessionFlow struct"
    - path: "gosysml2/sysml/parse.go"
      provides: "SuccessionFlowUsage handler"
      contains: "EnterSuccessionFlowUsage"
    - path: "gosysml2/sysml/visitor.go"
      provides: "VisitSuccessionFlow method"
      contains: "VisitSuccessionFlow.*SuccessionFlow"
    - path: "gosysml2/sysml/flow_test.go"
      provides: "Unit tests for SuccessionFlow"
      contains: "TestSuccessionFlow"
  key_links:
    - from: "parse.go EnterSuccessionFlowUsage"
      to: "model.go SuccessionFlow"
      via: "element creation"
      pattern: "NewSuccessionFlow"
    - from: "model.go SuccessionFlow"
      to: "Source/Target elements"
      via: "reference resolution"
      pattern: "resolveSuccessionFlowRefs"
    - from: "visitor.go"
      to: "SuccessionFlow type"
      via: "type assertion"
      pattern: "case \*SuccessionFlow:"
---

<objective>
Implement SuccessionFlowUsage to enable control flow connections in action bodies, completing the P2 medium priority grammar elements.

Purpose: SuccessionFlowUsage represents control flow succession relationships (distinct from SuccessionAsUsage which is already implemented). This enables modeling of action flows with succession semantics.

Output: Working SuccessionFlow type with parser handlers, completing Phase 4 grammar coverage targets.
</objective>

<execution_context>
@/home/daniel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/daniel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase summaries for patterns
@.planning/phases/04-advanced-features/04-01-SUMMARY.md
@.planning/phases/04-advanced-features/04-02-SUMMARY.md
@.planning/phases/03-grammar-completion/03-01-SUMMARY.md

# Grammar reference
@docs/bnf/SysML-textual-bnf.kebnf (line 829 for SuccessionFlowUsage)

# Existing implementations for reference
@gosysml2/sysml/model.go (Flow type pattern, lines ~900-950)
@gosysml2/sysml/parse.go (Flow handlers, EnterFlowUsage pattern)
@gosysml2/sysml/model.go (SuccessionAsUsage already implemented)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SuccessionFlow type to model.go</name>
  <files>gosysml2/sysml/model.go</files>
  <action>
    Add SuccessionFlow struct following the Flow pattern:
    1. Add KindSuccessionFlow constant to ElementKind enum
    2. Add "succession flow" to String() switch
    3. Create SuccessionFlow struct with:
       - baseElement
       - Source Ref[Element] (the element that succeeds)
       - Target Ref[Element] (the element that is succeeded by)
       - unresolvedSource string
       - unresolvedTarget string
       - Guard string (optional guard condition)
    4. Implement isUsage() method (SuccessionFlowUsage is always a usage)
    5. Create NewSuccessionFlow(name, loc) constructor
    
    Note: SuccessionFlowUsage is distinct from SuccessionAsUsage.
    SuccessionFlowUsage is used in action bodies for control flow,
    while SuccessionAsUsage is for general succession relationships.
    
    Reference Flow struct pattern (around line 900+ in model.go).
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>SuccessionFlow type defined with required fields</done>
</task>

<task type="auto">
  <name>Task 2: Add SuccessionFlow to visitor pattern</name>
  <files>gosysml2/sysml/visitor.go</files>
  <action>
    1. Add VisitSuccessionFlow(flow *SuccessionFlow) bool to Visitor interface
    2. Add VisitSuccessionFlow to BaseVisitor (returns true)
    3. Add VisitSuccessionFlow to Counter (increments KindSuccessionFlow count)
    4. Add case *SuccessionFlow to VisitElement switch statement
    5. Add FindSuccessionFlows function (follows FindFlows pattern)
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>Visitor pattern supports SuccessionFlow type</done>
</task>

<task type="auto">
  <name>Task 3: Implement SuccessionFlowUsage handler</name>
  <files>gosysml2/sysml/parse.go</files>
  <action>
    Implement EnterSuccessionFlowUsage following EnterFlowUsage pattern:
    1. Parse the succession flow usage context
    2. Extract source and target from the succession relationship
    3. Create SuccessionFlow element
    4. Set unresolvedSource and unresolvedTarget
    5. Add to current action or flow context
    
    The BNF shows SuccessionFlowUsage at line 829:
    SuccessionFlowUsage = FlowUsagePrefix 'succession' ...
    
    This is similar to FlowUsage but specifically for succession semantics.
    Parse the source and target from the relationship context.
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>SuccessionFlowUsage handler creates elements</done>
</task>

<task type="auto">
  <name>Task 4: Add SuccessionFlow reference resolution</name>
  <files>gosysml2/sysml/model.go</files>
  <action>
    1. Add ResolveReferences for SuccessionFlow:
       - Resolve Source reference from unresolvedSource
       - Resolve Target reference from unresolvedTarget
    2. Add to Model.ResolveReferences switch statement
    3. Add resolveSuccessionFlowRefs helper function
    
    Pattern follows resolveFlowRefs and resolveConnectionRefs.
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>SuccessionFlow references resolve correctly</done>
</task>

<task type="auto">
  <name>Task 5: Create unit tests</name>
  <files>gosysml2/sysml/flow_test.go</files>
  <action>
    Create comprehensive unit tests:
    1. TestSuccessionFlowUsage - parse basic succession flow
    2. TestSuccessionFlowWithSourceTarget - parse with explicit source/target
    3. TestSuccessionFlowResolution - verify references resolve
    4. TestSuccessionFlowInAction - parse succession within action body
    5. TestSuccessionFlowWithGuard - parse with guard condition
    
    Test with SysML like:
    ```
    action def MyAction {
      action a1;
      action a2;
      succession s from a1 to a2;
    }
    ```
    
    Or:
    ```
    action def MyAction {
      action a1;
      succession from a1;
      action a2;
    }
    ```
  </action>
  <verify>go test ./gosysml2/sysml/... -run TestSuccessionFlow -v passes</verify>
  <done>All SuccessionFlow tests pass with 100% coverage</done>
</task>

<task type="auto">
  <name>Task 6: Run validation suite to verify coverage</name>
  <files>gosysml2/sysml/</files>
  <action>
    1. Run the validation test suite to check success rate
    2. Verify grammar coverage increased
    3. Check no regressions in existing tests
    4. Document final coverage percentage
    
    Commands:
    - go test ./gosysml2/sysml/... -v
    - go test ./... (full test suite)
    - Run validation checker if available
  </action>
  <verify>All tests pass, coverage at 73%+, validation success rate 98%+</verify>
  <done>Phase 4 complete with target coverage achieved</done>
</task>

</tasks>

<verification>
- [ ] SuccessionFlow type exists in model.go
- [ ] KindSuccessionFlow constant defined and has String() case
- [ ] Visitor interface has VisitSuccessionFlow method
- [ ] EnterSuccessionFlowUsage handler creates elements
- [ ] SuccessionFlow references resolve (Source, Target)
- [ ] Unit tests pass for all SuccessionFlow scenarios
- [ ] Grammar coverage reaches 73%+ (7 elements in Phase 4)
- [ ] Validation success rate maintained at 98%+
</verification>

<success_criteria>
- SuccessionFlowUsage parses without discarding elements
- SuccessionFlow connects source and target elements correctly
- All unit tests pass
- Grammar coverage reaches 73%+ (up from 68%)
- Validation success rate maintained at 98%+
- Zero elements discarded during parsing
- Phase 4 complete: All P2 medium priority elements implemented
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-features/04-03-SUMMARY.md`
</output>

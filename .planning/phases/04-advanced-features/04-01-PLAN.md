---
phase: 04-advanced-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gosysml2/sysml/model.go
  - gosysml2/sysml/parse.go
  - gosysml2/sysml/visitor.go
  - gosysml2/sysml/case_test.go
autonomous: true
must_haves:
  truths:
    - "CaseDefinition elements parse and appear in model"
    - "CaseUsage elements parse and appear in model"
    - "Case elements have Subject, Actor, Objective members"
    - "Case elements resolve type references"
    - "All new handlers have unit tests"
  artifacts:
    - path: "gosysml2/sysml/model.go"
      provides: "Case type definition with KindCase"
      contains: "type Case struct"
    - path: "gosysml2/sysml/parse.go"
      provides: "CaseDefinition and CaseUsage handlers"
      contains: "EnterCaseDefinition|EnterCaseUsage"
    - path: "gosysml2/sysml/visitor.go"
      provides: "VisitCase method"
      contains: "VisitCase.*Case"
    - path: "gosysml2/sysml/case_test.go"
      provides: "Unit tests for Case parsing"
      contains: "TestCase"
  key_links:
    - from: "parse.go EnterCaseDefinition"
      to: "model.go Case struct"
      via: "element creation"
      pattern: "NewCase"
    - from: "parse.go EnterCaseUsage"
      to: "model.go Case struct"
      via: "element creation"
      pattern: "NewCase"
    - from: "visitor.go"
      to: "Case type"
      via: "type assertion"
      pattern: "case \*Case:"
---

<objective>
Implement Case modeling support (CaseDefinition and CaseUsage) to enable parsing of SysML case constructs.

Purpose: Case modeling is a core SysML construct for analysis, verification, and use case modeling. Currently the parser recognizes Case grammar rules but has no handlers, causing case elements to be silently discarded.

Output: Working Case type with parser handlers, following the same pattern as AnalysisCase and UseCase.
</objective>

<execution_context>
@/home/daniel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/daniel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase summary for patterns
@.planning/phases/03-grammar-completion/03-02-SUMMARY.md

# Grammar reference
@docs/bnf/SysML-textual-bnf.kebnf (lines 1499-1511 for CaseDefinition/Usage)

# Existing case implementations for reference
@gosysml2/sysml/model.go (AnalysisCase and UseCase patterns)
@gosysml2/sysml/parse.go (EnterAnalysisCaseDefinition/Usage handlers)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Case type to model.go</name>
  <files>gosysml2/sysml/model.go</files>
  <action>
    Add Case struct following the AnalysisCase pattern:
    1. Add KindCase constant to ElementKind enum (after KindOccurrence)
    2. Add "case" to String() switch
    3. Create Case struct with:
       - baseElement
       - IsDefinition bool
       - TypeRef Ref[*Case]
       - Subject Ref[Element]
       - unresolvedSubject string
       - Actors []Ref[Element]
       - unresolvedActors []string
       - Objectives []Ref[*Requirement]
       - unresolvedObjectives []string
    4. Implement isDefinition(), isUsage(), Type() methods
    5. Create NewCase(name, loc, isDefinition) constructor
    
    Pattern follows AnalysisCase exactly (lines 896-931 in model.go).
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>Case type defined with all required fields and methods</done>
</task>

<task type="auto">
  <name>Task 2: Add Case to visitor pattern</name>
  <files>gosysml2/sysml/visitor.go</files>
  <action>
    1. Add VisitCase(case *Case) bool method to Visitor interface
    2. Add VisitCase to BaseVisitor (returns true)
    3. Add VisitCase to Counter (increments KindCase count)
    4. Add case *Case to VisitElement switch statement
    5. Add FindCases function (follows FindUseCases pattern)
    
    Reference existing patterns from VisitAnalysisCase and VisitUseCase.
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>Visitor interface and implementations support Case type</done>
</task>

<task type="auto">
  <name>Task 3: Implement CaseDefinition handler</name>
  <files>gosysml2/sysml/parse.go</files>
  <action>
    Implement EnterCaseDefinition following EnterAnalysisCaseDefinition pattern:
    1. Extract name from DefinitionDeclaration
    2. Create Case with isDefinition=true
    3. Set as current element
    4. Add to current package
    
    Also implement ExitCaseDefinition for subject/actor/objective resolution.
    
    Note: CaseBody contains SubjectMember, ActorMember, ObjectiveMember.
    These need to be parsed and stored in the Case (similar to how
    UseCase handles includedUseCases).
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>CaseDefinition handler creates elements and parses body</done>
</task>

<task type="auto">
  <name>Task 4: Implement CaseUsage handler</name>
  <files>gosysml2/sysml/parse.go</files>
  <action>
    Implement EnterCaseUsage following EnterAnalysisCaseUsage pattern:
    1. Extract name from ConstraintUsageDeclaration
    2. Create Case with isDefinition=false
    3. Set as current element
    4. Add to current parent
    
    Also implement ExitCaseUsage for resolution.
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>CaseUsage handler creates elements and parses body</done>
</task>

<task type="auto">
  <name>Task 5: Add Case reference resolution</name>
  <files>gosysml2/sysml/model.go</files>
  <action>
    1. Add unresolvedSubject, unresolvedActors, unresolvedObjectives fields
    2. Add ResolveReferences method for Case (follow resolveAnalysisCaseRefs pattern)
    3. Resolve Subject, Actors, and Objectives from their unresolved string slices
    4. Call from Model.ResolveReferences switch statement
  </action>
  <verify>go build ./gosysml2/sysml/... compiles without errors</verify>
  <done>Case references resolve correctly after parsing</done>
</task>

<task type="auto">
  <name>Task 6: Create unit tests</name>
  <files>gosysml2/sysml/case_test.go</files>
  <action>
    Create comprehensive unit tests following existing test patterns:
    1. TestCaseDefinition - parse simple case definition
    2. TestCaseUsage - parse case usage
    3. TestCaseWithSubject - parse case with subject member
    4. TestCaseWithActor - parse case with actor member
    5. TestCaseWithObjective - parse case with objective requirement
    6. TestCaseResolution - verify references resolve
    
    Use test files from validationdata that contain case constructs.
  </action>
  <verify>go test ./gosysml2/sysml/... -run TestCase -v passes</verify>
  <done>All Case tests pass with 100% coverage</done>
</task>

</tasks>

<verification>
- [ ] Case type exists in model.go with all required fields
- [ ] KindCase constant defined and has String() case
- [ ] Visitor interface has VisitCase method
- [ ] EnterCaseDefinition handler creates Case elements
- [ ] EnterCaseUsage handler creates Case elements
- [ ] Case references resolve (Subject, Actors, Objectives)
- [ ] Unit tests pass for all Case scenarios
- [ ] No elements discarded when parsing case constructs
</verification>

<success_criteria>
- CaseDefinition and CaseUsage elements parse without discarding
- Case elements have proper Subject, Actor, Objective members
- Reference resolution works for case elements
- Grammar coverage increases from 68% to ~70% (2 new elements)
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-features/04-01-SUMMARY.md`
</output>

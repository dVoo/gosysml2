---
phase: 05-documentation-and-examples
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [
  "gosysml2/examples/basic/main.go",
  "gosysml2/examples/requirements/main.go",
  "gosysml2/examples/validation/main.go",
  "gosysml2/examples/parallel/main.go",
  "gosysml2/examples/visitor/main.go"
]
autonomous: true

must_haves:
  truths:
    - "Each example demonstrates a specific use case"
    - "All examples compile and run successfully"
    - "Examples cover common parsing scenarios"
  artifacts:
    - path: "gosysml2/examples/basic/main.go"
      provides: "Basic parsing example"
      min_lines: 40
    - path: "gosysml2/examples/requirements/main.go"
      provides: "Requirements parsing example"
      min_lines: 50
    - path: "gosysml2/examples/validation/main.go"
      provides: "Validation example"
      min_lines: 30
    - path: "gosysml2/examples/parallel/main.go"
      provides: "Parallel parsing example"
      min_lines: 40
    - path: "gosysml2/examples/visitor/main.go"
      provides: "Visitor pattern example"
      min_lines: 50
  key_links:
    - from: "gosysml2/examples/*/main.go"
      to: "gosysml2/sysml"
      via: "import"
      pattern: "github.com/dVoo/gosysml2/sysml"
---

<objective>
Create comprehensive code examples demonstrating different library usage patterns.

Purpose: Provide working code examples for common use cases that developers can copy and adapt
Output: 5 example programs in gosysml2/examples/ subdirectories
</objective>

<execution_context>
@/home/daniel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/daniel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@gosysml2/examples/main.go
@gosysml2/README.md
@gosysml2/sysml/parse.go
@gosysml2/sysml/visitor.go
</context>

<tasks>

<task type="auto">
  <name>Create basic parsing example</name>
  <files>gosysml2/examples/basic/main.go</files>
  <action>
Create a basic parsing example that demonstrates:
- Parsing a simple SysML model from string
- Accessing packages and parts
- Using FindParts and FindRequirements
- Error handling

Include a self-contained SysML example (Vehicle model with parts).
Add comments explaining each step.
  </action>
  <verify>cd gosysml2/examples/basic && go run main.go compiles and runs</verify>
  <done>Basic example runs successfully and outputs parsed elements</done>
</task>

<task type="auto">
  <name>Create requirements traceability example</name>
  <files>gosysml2/examples/requirements/main.go</files>
  <action>
Create an example focused on requirements:
- Parse a model with requirements
- Show requirement relationships (derivedFrom, satisfiedBy, verifiedBy)
- Demonstrate requirement ID extraction
- Show how to find requirements by name

Include a realistic requirements model with traceability.
  </action>
  <verify>cd gosysml2/examples/requirements && go run main.go compiles and runs</verify>
  <done>Requirements example demonstrates traceability relationships</done>
</task>

<task type="auto">
  <name>Create validation example</name>
  <files>gosysml2/examples/validation/main.go</files>
  <action>
Create an example showing validation:
- Validate SysML syntax without full parsing
- Show error handling for invalid input
- Demonstrate error location reporting

Include both valid and invalid examples.
  </action>
  <verify>cd gosysml2/examples/validation && go run main.go compiles and runs</verify>
  <done>Validation example shows syntax checking and error reporting</done>
</task>

<task type="auto">
  <name>Create parallel parsing example</name>
  <files>gosysml2/examples/parallel/main.go</files>
  <action>
Create an example demonstrating parallel parsing:
- Parse multiple files concurrently
- Use ParseDirectoryParallel
- Show worker configuration
- Aggregate results from multiple files

Create sample files or reference validationdata/ for testing.
  </action>
  <verify>cd gosysml2/examples/parallel && go run main.go compiles and runs</verify>
  <done>Parallel example demonstrates concurrent file parsing</done>
</task>

<task type="auto">
  <name>Create visitor pattern example</name>
  <files>gosysml2/examples/visitor/main.go</files>
  <action>
Create an example showing custom visitor implementation:
- Implement a custom visitor struct
- Count specific element types
- Filter elements by criteria
- Show depth tracking during walk

Make it practical (e.g., find all requirements without IDs).
  </action>
  <verify>cd gosysml2/examples/visitor && go run main.go compiles and runs</verify>
  <done>Visitor example demonstrates custom traversal logic</done>
</task>

</tasks>

<verification>
- All examples compile: `cd gosysml2/examples/* && go build` succeeds
- All examples run without errors
- Each example has a clear README comment at the top explaining what it demonstrates
- Examples use realistic SysML models
</verification>

<success_criteria>
- 5 working example programs in gosysml2/examples/
- Each example demonstrates a specific use case
- All examples compile and run successfully
- Examples referenced from main README
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-and-examples/05-02-SUMMARY.md`
</output>

---
phase: 02-sysml-standard-libraries
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - gosysml2/sysml/integration_test.go
  - gosysml2/check_validation.go
autonomous: true
must_haves:
  truths:
    - All 18 validation categories are tested
    - Validation test results show pass/fail per category
    - Parser successfully resolves standard library imports in validation files
  artifacts:
    - path: gosysml2/sysml/integration_test.go
      provides: Updated integration tests for validationdata
      contains: "TestValidationCategories"
    - path: gosysml2/check_validation.go
      provides: Standalone validation checker
      exports: ["main"]
  key_links:
    - from: Integration tests
      to: validationdata/*.sysml
      via: ParseDirectory
      pattern: "validationdata/.*\\.sysml"
---

<objective>
Create comprehensive validation test suite that runs the parser against all 18 categories of SysML validation files.

Purpose: Verify that the parser with library support can successfully parse real-world SysML models using standard library definitions across all validation categories.

Output: Updated integration tests and standalone validation checker that report parsing success/failure per category.
</objective>

<execution_context>
@/home/daniel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/daniel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@gosysml2/sysml/integration_test.go
@gosysml2/check_testdata.go
@.planning/phases/02-sysml-standard-libraries/02-02-SUMMARY.md

## Validation Data Structure

18 categories in `./validationdata/`:
1. 01-Parts Tree - Basic part hierarchies
2. 02-Parts Interconnection - Connections between parts
3. 03-Function-based Behavior - Actions, functions
4. 04-Functional Allocation - Allocate behavior to structure
5. 05-State-based Behavior - State machines
6. 06-Individual and Snapshots - Time-based modeling
7. 07-Variant Configuration - Variants, configurations
8. 08-Requirements - Requirements modeling
9. 09-Verification - Verification cases
10. 10-Analysis and Trades - Analysis cases
11. 11-View and Viewpoint - Views
12. 12-Dependency Relationships - Dependencies
13. 13-Model Containment - Package structure
14. 14-Language Extensions - Custom extensions
15. 15-Properties-Values-Expressions - Properties
16. 17-Sequence Modeling - Sequences (note: no 16)
17. 18-Use Case - Use cases

Total: 56 .sysml files

## Current Integration Tests

integration_test.go currently:
- Points to `testdata` directory (needs update to `validationdata`)
- Tests ParseDirectory, ParseDirectoryParallel, ParseDirectoryStream
- Collects statistics but doesn't categorize by validation type
- Skips on parse errors (some files may be intentionally complex)

## Key Requirements

1. Update path from `testdata` to `validationdata`
2. Group results by category (01-Parts Tree, etc.)
3. Track library resolution success/failure
4. Report per-category statistics
5. Create standalone checker like check_testdata.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Integration Tests for Validation Data</name>
  <files>gosysml2/sysml/integration_test.go</files>
  <action>
Update integration tests to use validationdata and categorize results:

1. Update `integrationTestdataDir` function:
   - Change path from `"..", "..", "testdata"` to `"..", "..", "validationdata"`
   - Keep fallback logic for missing directory

2. Create `TestValidationCategories`:
   - Discover all validation files grouped by category
   - For each category (01-Parts Tree, 02-Parts Interconnection, etc.):
     - Parse all files in category with WithStandardLibrary()
     - Track statistics:
       - Files parsed successfully
       - Files with errors
       - Total elements parsed
       - Library imports resolved
   - Print category summary table
   - Report overall pass rate

3. Update existing tests to use validationdata:
   - TestIntegrationFullValidation
   - TestIntegrationParallelParsing
   - TestIntegrationStreaming
   - All directory-based tests

4. Add category-specific subtests:
   - Use t.Run(categoryName, ...) for each category
   - This allows running specific category: `go test -run TestValidationCategories/01`

5. Track library-specific metrics:
   - Count of library imports per file
   - Count of library elements referenced
   - Library resolution success rate

6. Handle missing standard library gracefully:
   - Skip tests if ./libraries/sysml.library not present
   - Log warning about limited test coverage

Update all path references from "testdata" to "validationdata".
  </action>
  <verify>go test -v ./gosysml2/sysml/... -run TestValidationCategories 2>&1 | head -80</verify>
  <done>Integration tests updated to use validationdata with category breakdown</done>
</task>

<task type="auto">
  <name>Task 2: Create Standalone Validation Checker</name>
  <files>gosysml2/check_validation.go</files>
  <action>
Create standalone validation checker similar to check_testdata.go:

1. Create `check_validation.go` in gosysml2/ directory:
   ```go
   package main
   
   import (
       "fmt"
       "os"
       "path/filepath"
       "strings"
       "github.com/dVoo/gosysml2/sysml"
   )
   ```

2. Implement main function:
   - Load standard library from ./libraries/sysml.library
   - Walk through ./validationdata/ directory
   - For each .sysml file:
     - Parse with library support
     - Record success/failure
     - Count elements, library references
   
3. Group results by category:
   - Extract category from directory name (e.g., "01-Parts Tree")
   - Aggregate statistics per category
   - Calculate success rate per category

4. Output formatted report:
   ```
   === SysML Validation Test Results ===
   
   Category                | Files | Parsed | Failed | Success Rate
   ------------------------+-------+--------+--------+-------------
   01-Parts Tree           |   4   |   4    |   0    |   100%
   02-Parts Interconnection|   3   |   3    |   0    |   100%
   ...
   
   Overall: 45/56 files parsed successfully (80%)
   ```

5. Add flags:
   - `-library-path` - custom library path
   - `-validation-path` - custom validation data path
   - `-verbose` - detailed per-file output
   - `-category` - test specific category only

6. Exit codes:
   - 0 if all files parse successfully
   - 1 if any files fail (for CI/CD integration)
   - 2 if library not found

Follow patterns from check_testdata.go but with category grouping.
  </action>
  <verify>go run ./gosysml2/check_validation.go 2>&1 | head -50</verify>
  <done>Standalone validation checker created with category reporting</done>
</task>

<task type="auto">
  <name>Task 3: Add Library Resolution Verification Tests</name>
  <files>gosysml2/sysml/integration_test.go</files>
  <action>
Add specific tests to verify library resolution in validation files:

1. Test `TestLibraryImportResolution`:
   - Parse sample validation file with library imports
   - Verify imports are resolved to actual library packages
   - Check that Import.ResolvedPackage is set

2. Test `TestLibraryElementReferences`:
   - Parse file with qualified name references (ISQ::mass, etc.)
   - Verify references resolve to library elements
   - Check TypeRef.Resolved() returns non-nil for library types

3. Test `TestValidationFileLibraryUsage`:
   - Select representative files from each category:
     - 01-Parts Tree: Has SI::kg, ISQ::mass imports
     - 03-Function-based Behavior: Has action/function imports
     - 15-Properties-Values-Expressions: Has ScalarValues imports
   - Parse with library support
   - Verify library elements are accessible
   - Log resolution statistics

4. Test `TestLibraryPerformance`:
   - Measure time to load standard library (should be once, cached)
   - Measure parse time for files with/without library resolution
   - Verify library loading doesn't significantly impact parse time

5. Test `TestLibraryErrorHandling`:
   - Parse file with invalid library import
   - Verify graceful error (not panic)
   - Check error message mentions unresolved import

6. Skip tests if library not available:
   - Use t.Skip with descriptive message
   - Don't fail CI if library optional

Add these to integration_test.go following existing patterns.
  </action>
  <verify>go test -v ./gosysml2/sysml/... -run "TestLibrary.*Resolution|TestLibrary.*Reference" 2>&1 | tail -40</verify>
  <done>Library resolution verification tests added and passing</done>
</task>

<task type="auto">
  <name>Task 4: Create Validation Summary Report</name>
  <files>gosysml2/check_validation.go</files>
  <action>
Enhance validation checker with detailed reporting:

1. Add detailed error reporting:
   - For each failed file, show:
     - File path
     - Error message (first error)
     - Line/column if available
     - Category

2. Add library-specific reporting:
   - List all library packages imported
   - Show library element reference counts
   - Report unresolved library references

3. Generate summary statistics:
   - Total files processed
   - Files parsed successfully
   - Files with errors
   - Average elements per file
   - Library coverage (files using libraries / total files)

4. Add JSON output option:
   - `-json` flag outputs structured results
   - Useful for CI/CD integration and automated tracking
   - Include: categories, files, success status, element counts

5. Add comparison mode:
   - `-compare` flag with previous results file
   - Show improvement/regression vs baseline
   - Track progress over time

6. Create VALIDATION.md template:
   - Document expected results
   - Known issues (files expected to fail)
   - Performance baselines

7. Ensure check_validation.go can be run:
   - `go run ./gosysml2/check_validation.go` from repo root
   - Or build: `go build -o check_validation ./gosysml2/check_validation.go`

Focus on actionable output - clear pass/fail with specific details.
  </action>
  <verify>go run ./gosysml2/check_validation.go -verbose 2>&1 | tail -60</verify>
  <done>Validation checker produces detailed reports with library metrics</done>
</task>

</tasks>

<verification>
- Integration tests use validationdata directory
- Tests categorize results by validation category (01-Parts Tree, etc.)
- Standalone check_validation.go runs successfully
- Library imports resolve in validation files
- Category pass rates are reported
- JSON output option works
- All existing tests continue to pass
</verification>

<success_criteria>
- TestValidationCategories runs and reports per-category statistics
- check_validation.go executable produces formatted results
- Library imports in validation files resolve to actual elements
- Overall pass rate for validation files is calculated and reported
- Tests can be run with: go test -run TestValidationCategories ./gosysml2/sysml/...
</success_criteria>

<output>
After completion, create `.planning/phases/02-sysml-standard-libraries/02-03-SUMMARY.md`
</output>
